# Lekkantu inside M221Tech

The product itself, its database, application code and canonical logo remain in `mosn221/lekkantu`.

This repository only mirrors the exact approved logo and generates the presentation derivatives required by M221Tech. That separation avoids creating a second Lekkantu product stack inside the corporate site.
