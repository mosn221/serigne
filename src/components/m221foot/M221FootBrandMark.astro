---
interface Props {
  size?: number;
  className?: string;
  decorative?: boolean;
  alt?: string;
}

const {
  size = 78,
  className = '',
  decorative = true,
  alt = 'M221Foot'
} = Astro.props;
---

<img
  class={className}
  src="/assets/m221foot/mark.svg"
  width={size}
  height={size}
  alt={decorative ? '' : alt}
  aria-hidden={decorative ? 'true' : undefined}
  loading="eager"
  decoding="async"
/>
